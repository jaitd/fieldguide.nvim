*.log
.luarc.json
doc/tags
node_modules/
mise.local.toml
.mise.local.toml

# Built indexes. The artifact is released, never committed.
nvim-plugins.db
nvim-plugins.db.gz

# Takes. A cast is cheap to record again; commit a rendered gif on purpose.
demo/fieldguide.cast
