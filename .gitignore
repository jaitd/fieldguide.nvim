*.log
.luarc.json
doc/tags
node_modules/
mise.local.toml
.mise.local.toml

# Built indexes. The artifact is released, never committed.
nvim-plugins.db
nvim-plugins.db.gz

# The demo profile: a throwaway Neovim config tree, its plugins and its index.
demo/.profile/
# Takes. A cast is cheap to record again; commit a rendered gif on purpose.
demo/fieldguide.cast
